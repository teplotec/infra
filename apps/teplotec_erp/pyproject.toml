[project]
name = "teplotec_erp"
authors = [
  { name = "TeploTEC" }
]
description = "TeploTEC extensions and Ukrainian localization for ERPNext"
requires-python = ">=3.12"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"
erpnext = ">=16.0.0,<17.0.0"
